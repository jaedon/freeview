/*
 * Please do not edit this file.
 * It was generated using rpcgen.
 */

#include "../Common/dsgccClientRegistration_rpc.h"
#include <stdio.h>
#include <stdlib.h>
#include <rpc/pmap_clnt.h>
#include <string.h>
#include <memory.h>
#include <sys/socket.h>
#include <netinet/in.h>

#ifndef SIG_PF
#define SIG_PF void(*)(int)
#endif

void
dsgregistrar_1(struct svc_req *rqstp, register SVCXPRT *transp)
{
	union {
		struct dsgClientRegInfo registerdsgclient_1_arg;
		struct dsgClientRegInfo unregisterdsgclient_1_arg;
		struct dsgClientRegInfo keepalivedsgclient_1_arg;
		struct dsgClientClassifierRegInfo registerdsgclientwithclassifier_1_arg;
	} argument;
	char *result;
	xdrproc_t _xdr_argument, _xdr_result;
	char *(*local)(char *, struct svc_req *);

	switch (rqstp->rq_proc) {
	case NULLPROC:
		(void) svc_sendreply (transp, (xdrproc_t) xdr_void, (char *)NULL);
		return;

	case REGISTERDSGCLIENT:
		_xdr_argument = (xdrproc_t) xdr_dsgClientRegInfo;
		_xdr_result = (xdrproc_t) xdr_int;
		local = (char *(*)(char *, struct svc_req *)) registerdsgclient_1_svc;
		break;

	case UNREGISTERDSGCLIENT:
		_xdr_argument = (xdrproc_t) xdr_dsgClientRegInfo;
		_xdr_result = (xdrproc_t) xdr_int;
		local = (char *(*)(char *, struct svc_req *)) unregisterdsgclient_1_svc;
		break;

	case KEEPALIVEDSGCLIENT:
		_xdr_argument = (xdrproc_t) xdr_dsgClientRegInfo;
		_xdr_result = (xdrproc_t) xdr_int;
		local = (char *(*)(char *, struct svc_req *)) keepalivedsgclient_1_svc;
		break;

	case REGISTERDSGCLIENTWITHCLASSIFIER:
		_xdr_argument = (xdrproc_t) xdr_dsgClientClassifierRegInfo;
		_xdr_result = (xdrproc_t) xdr_int;
		local = (char *(*)(char *, struct svc_req *)) registerdsgclientwithclassifier_1_svc;
		break;

	default:
		svcerr_noproc (transp);
		return;
	}
	memset ((char *)&argument, 0, sizeof (argument));
	if (!svc_getargs (transp, (xdrproc_t) _xdr_argument, (caddr_t) &argument)) {
		svcerr_decode (transp);
		return;
	}
	result = (*local)((char *)&argument, rqstp);
	if (result != NULL && !svc_sendreply(transp, (xdrproc_t) _xdr_result, result)) {
		svcerr_systemerr (transp);
	}
	if (!svc_freeargs (transp, (xdrproc_t) _xdr_argument, (caddr_t) &argument)) {
		fprintf (stderr, "%s", "unable to free arguments");
		exit (1);
	}
	return;
}
