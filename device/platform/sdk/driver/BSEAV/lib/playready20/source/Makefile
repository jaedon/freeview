DRM_BUILD_ARCH=X86
DRM_BUILD_PLATFORM=ANSI
DRM_BUILD_PROFILE=OEM
DRM_BUILD_TYPE=CHECKED
DRM_SOURCE_PATH=$(shell pwd)
DRM_ROOT_PATH=$(subst /source,,$(DRM_SOURCE_PATH))

export DRM_SOURCE_PATH DRM_ROOT_PATH

include ./dirs

include $(DRM_SOURCE_PATH)/default.mak
