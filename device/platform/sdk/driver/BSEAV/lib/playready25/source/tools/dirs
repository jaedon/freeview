#-----------------------------------------------------------------------------
# This file is read by both the NTBUILD tool and the Porting Kit's makefile
# system. The latter requires the order of directories to be:
#     1st - Directories that build headers
#     2nd - Directories that build objects
#     3rd - Directories that build libraries
#     4th - Directories that build executables and DLLs
# This is analagous to the PASS0, PASS1a/1b, and PASS2 phases of NTBUILD.
#
# If one lib packages up another, the final lib must appear later in the list.
#
# DIRS is read by NTBUILD and the Porting Kit if DRM_DIRS isn't present.
#
# DRM_DIRS is the optional override for use by the Porting Kit. Directory
# selection can be restricted by platform, architecture, or a combo as follows:
# DRM_DIRS=foo{ARM} bar{ANSI} baz{ANSI_X86}
#
# Supported tags (out of the box):
# - Platform: ANSI
# - Architecture: X86, AMD64/X64, ARM
# - PlatArch: Platform and Arch separated by an underscore (e.g. ANSI_X86)
#-----------------------------------------------------------------------------

DIRS =                                                          \
    common                                                      \
    perftool{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}           \
    chkhds{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}             \
    license{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}            \
    generatekeypair{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}    \
    errsay{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}             \
    headerverify{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}       \
    metering{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}           \
    player{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}             \
    synclist{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}           \
    devicedevcert{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}      \
    profilescope{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}       \
    otadla{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}             \
    metercertstore{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}     \
    secure_clock{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}       \
    drmfileviewer{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}      \
    embeddingtool{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}      \
    tracetool{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}          \
    calcchecksum{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}       \
    deviceexclusion{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}    \
    drmviewer{!_WINCEROOT,!PPC,!ARM,!PLAYREADY_PRODUCT_WP8}     \
    keyfile{!_WINCEROOT,!PLAYREADY_PRODUCT_WP8}                 \
    package{!PPC,!PLAYREADY_PRODUCT_WP8}                        \
    perfxml{!PPC,!ARM,!PLAYREADY_PRODUCT_WP8}                   \
    xmr2xml{!_WINCEROOT,!PPC,!PLAYREADY_PRODUCT_WP8}            \
    m2tsencryptor{!PPC,!ARM,!PLAYREADY_PRODUCT_WP8}             \
    prnd{!PPC,!ARM,!PLAYREADY_PRODUCT_WP8}                      \

DRM_DIRS =                                                      \
    common                                                      \
    errsay                                                      \
    perftool                                                    \
    chkhds                                                      \
    license                                                     \
    generatekeypair                                             \
    headerverify                                                \
    metering                                                    \
    player{!DRM_SUPPORT_SECUREOEMHAL_PLAY_ONLY}                 \
    synclist{!DRM_SUPPORT_SECUREOEMHAL_PLAY_ONLY}               \
    devicedevcert                                               \
    profilescope                                                \
    otadla                                                      \
    metercertstore                                              \
    secure_clock                                                \
    drmfileviewer                                               \
    embeddingtool                                               \
    tracetool{X86,AMD64}                                        \
    xmr2xml                                                     \
    m2tsencryptor{!DRM_SUPPORT_SECUREOEMHAL_PLAY_ONLY}          \
    prnd{X86,AMD64}                                             \

