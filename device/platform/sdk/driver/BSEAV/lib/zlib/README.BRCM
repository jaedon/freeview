libz-1.3.1

To rebuild this library from the src.tgz tarball in this directory, simply unzip and untar it, run ./configure, and make.

The library should appear in a directory for the architecture you are using.

The license files are included in the tarball.

